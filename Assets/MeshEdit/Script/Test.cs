using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
    void Start()
    {
        //Debug.Log(MathfTool.GetVectorRad(Vector3.zero, new Vector3(2, -1, 0)) * Mathf.Rad2Deg);
	
	}
	
	// Update is called once per frame
	void Update () {
	}
}
